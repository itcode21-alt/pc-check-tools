# Mac 로컬 사이트 관리

`ITSVC-Site-Manager.command`는 공개 관리자 페이지에 접속하지 않고 이 맥북에서 `itsvc.co.kr`을 관리하는 도구입니다.

## 실행

1. Finder에서 사이트 저장소 폴더를 엽니다.
2. `ITSVC-Site-Manager.command`를 더블클릭합니다.
3. 터미널 메뉴에서 필요한 번호를 선택합니다.

처음 실행할 때 macOS가 차단하면 파일을 Control-클릭하고 **열기**를 선택합니다.

## 제공 기능

- Git 변경 및 최근 커밋 확인
- `http://localhost:8811` 로컬 미리보기 실행과 종료
- JavaScript 문법, 내부 링크, 필수 파일, Git 형식 검사
- GitHub 원격 저장소와 안전하게 동기화
- 변경 파일 확인, 커밋, GitHub Pages 배포
- 홈, 진단, 가이드, 사이트맵, `ads.txt` 운영 상태 확인
- Finder에서 프로젝트 폴더 열기

## 브라우저 관리자 메뉴

터미널 대신 맥북 브라우저에서 관리하려면 맥미니에서 다음처럼 실행합니다.

```bash
export ADMIN_TOKEN='긴-임의-문자열'
node scripts/admin-server.mjs
```

그 다음 `http://127.0.0.1:8833`을 엽니다. 맥북에서 접속할 때는 맥미니와 맥북을 Tailscale로 연결한 뒤 `ADMIN_HOST`를 내부 IP에 바인딩하고, 방화벽에서 해당 내부망만 허용하세요. 관리자 페이지를 공개 도메인에 배포하지 마세요.

브라우저 메뉴에서는 Git 상태, 품질 검사, 공개 사이트 확인, GitHub 반영을 실행할 수 있습니다. SSH 키는 기존 Git 설정을 그대로 사용합니다.

## 안전 원칙

- `git push --force`를 사용하지 않습니다.
- 원격 저장소가 앞서 있고 로컬 수정도 있으면 자동 병합하지 않습니다.
- 검사에 실패하면 배포하지 않습니다.
- 커밋할 파일 목록을 표시하고 확인을 받은 뒤 배포합니다.
- `$HOME/.ssh/id_ed25519_codex`가 있으면 해당 SSH 키를 사용합니다.
- API 키와 `.env` 파일은 Git에 포함하지 않습니다.

사이트 글과 디자인 수정은 기존처럼 Codex 또는 편집기에서 진행하고, 이 도구로 미리보기·검사·배포하면 됩니다.
