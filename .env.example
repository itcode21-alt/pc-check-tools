# 실제 값은 저장소 루트에 .env 파일로 만들어서 넣으세요 (.gitignore 처리되어 있어 커밋되지 않습니다).
# scripts/enrich-symptoms-with-gemini.mjs 등 콘텐츠 생성 스크립트가 사용합니다.
GEMINI_API_KEY=
# 모델 이름이 만료/변경되면 이 값만 바꾸면 됩니다.
GEMINI_MODEL=gemini-2.5-flash
