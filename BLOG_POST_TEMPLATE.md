# 네이버 블로그(itcode21) 포스팅 템플릿 — 속보→심화 구조

블로그와 itsvc.co.kr(PC 윈도우 진단 센터)의 역할을 아래처럼 고정한다.

- **블로그**: 이슈가 뜬 시점에 가장 빠르게 색인·노출되는 채널. "이번 주에 이런 문제가 있다"를 먼저 다룬다.
- **사이트**: 공식 문서로 교차 확인한 뒤 구조화된 영구 레퍼런스로 남긴다. 증상·오류코드별로 정리되어 오래 검색 유입을 받는다.
- **연결**: 블로그 글 하단에 사이트의 관련 가이드를 "참고 자료"로 링크한다(이미 하고 있는 패턴, 계속 유지). 사이트 news.html에도 "초기 상황은 블로그에서 먼저 다룬다"는 역링크를 걸어 뒀다.

새 이슈를 발견하면 아래 순서로 진행한다.

1. **블로그를 먼저 쓴다.** 공식 문서 확인 전이라도 증상·재현 조건·현재까지 확인된 정보를 정리해서 빠르게 올린다. 시의성이 곧 노출 순위다.
2. **사이트 페이지가 없으면 나중에 만든다.** 같은 주제로 사이트에 오류코드/증상 페이지가 이미 있으면 3번으로. 없으면 블로그 포스팅 축적 후 별도로 `guides.html` 계열 페이지를 신설한다(itsvc.co.kr 쪽 작업).
3. **사이트 가이드가 생기거나 이미 있으면 블로그 하단에 참고 자료 링크를 건다.**
4. **사이트 쪽 news.html / windows-update-tracker.html 등도 공식 문서 확인 후 갱신한다.**

## 제목 규칙

- 핵심 키워드(오류 메시지, 오류 코드, 증상)를 제목 앞쪽에 그대로 넣는다. "PC진단", "컴퓨터진단" 같은 포괄 키워드는 검색량이 거의 없으니 쓰지 않는다.
- 예: `Windows 11 업데이트 0x800f0922 오류 해결 방법, 35%에서 되돌아간다면 확인할 것`
- 예: `컴퓨터 갑자기 꺼졌다 켜짐? Kernel-Power 41 오류 원인과 해결 방법`
- 패턴: `[증상/오류코드 그대로] + [원인/해결 방법] + [부가 조건이나 체크포인트]`

## 본문 구조

```
1. 증상 요약 (2~3문장)
   - 사용자가 실제로 보는 화면/메시지를 그대로 인용
   - 언제/어떤 상황에서 발생하는지

2. 원인 후보 (표 또는 목록)
   - 소프트웨어 원인 / 하드웨어 원인 나눠서 정리
   - 이 시점에 "단정하지 않기" — 사이트 운영 원칙과 동일하게 유지

3. 확인 순서 (번호 목록, 쉬운 것부터)
   - 데이터 손실 위험이 있는 작업은 뒤로 미룬다
   - 스크린샷/캡처 이미지 최소 1~2장 (직접 캡처, 스톡 이미지 금지)

4. 해결 방법 (단계별)
   - 각 단계마다 "왜 이걸 하는지" 한 줄 설명 추가

5. 참고 자료 (하단 고정 블록)
   참고 자료: PC 윈도우 진단 센터 - [관련 가이드 제목](사이트 URL)

6. 이웃 소통 유도 (선택, 시리즈물일 때)
   - "다음 편에서는 ~ 다루겠습니다. 이웃추가 해두시면 놓치지 않습니다."
```

## 참고 자료 링크 문구 고정 포맷

블로그에서 이미 쓰고 있는 패턴을 그대로 유지한다.

```
참고 자료: PC 윈도우 진단 센터 - {가이드 제목}
{사이트 URL}
```

- 앵커 텍스트는 항상 "PC 윈도우 진단 센터"로 고정해서 브랜드 검색에도 기여하게 한다.
- 링크 대상은 해당 포스팅과 가장 가까운 사이트 페이지 1개만 건다(여러 개 걸지 않기 — 링크주스 분산 방지).
- 사이트에 아직 해당 가이드가 없으면 이 블록을 비워두지 말고, 가장 근접한 상위 카테고리 페이지(예: `guides.html`)로 임시 연결한다.

## 체크리스트 (발행 전)

- [ ] 제목에 오류코드/증상 키워드가 그대로 들어갔는가
- [ ] 실제 캡처 이미지가 있는가 (스톡/타 사이트 이미지 아님)
- [ ] 원인을 확정적으로 말하지 않고 "후보"로 표현했는가
- [ ] 하단에 itsvc.co.kr 관련 가이드 링크 1개를 걸었는가
- [ ] 같은 주제가 이미 사이트에 있으면 그 페이지와 표현(용어)을 통일했는가
- [ ] PC/Windows/게임 오류 범주를 벗어난 주제가 아닌가 (블로그 주제 집중도 유지)
